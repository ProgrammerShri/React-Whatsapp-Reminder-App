import { Input } from 'antd'
import React from 'react'

const AppInput = () => {
  return (
    <Input style={{
      border: '1px solid #ccc',
      borderRadius: '30px',
      height: '40px',
      width: '300px',
    }} />
  )
}

export default AppInput